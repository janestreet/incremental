(library
 ((name incremental_lib)
  (public_name incremental)
  (libraries (core
              incremental_kernel
              core.weak_hashtbl))
  (preprocess (pps (ppx_jane -check-doc-comments ppxlib.runner)))
  ))


(jbuild_version 1)
